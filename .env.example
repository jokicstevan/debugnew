# ── GRPS Local Backend — copy to .env and fill in values ──────────────────────

# Flask session secret — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=Kretenboza123!

# PostgreSQL connection string (local or remote)
# Local example:  postgresql://grps_user:yourpass@localhost:5432/grps
# Render DB example: postgresql://grps_user:xxx@dpg-xxx.render.com/grps
DATABASE_URL=postgresql://results_nhg0_user:aZI18NxeXeCgqJYZp6rFlT7i8KZ23YBK@dpg-d85jsvgg4nts7383a8m0-a/results_nhg0

# Render frontend URL — used for CORS allow-list
# Set to your Render static site URL (no trailing slash)
RENDER_FRONTEND_URL=https://debugnew-64g2.onrender.com/

# HERE Maps API key (optional — OSRM is used as fallback)
# HERE_API_KEY=E8im9H87qOrwmc_FZID_0ixuUGZCjN0G0SmaQuK544Q

# OSRM routing server
# Leave blank to use the public demo server (rate-limited)
# For local OSRM (Docker): OSRM_URL=http://localhost:5000
# For Serbia map locally:  docker run -t -i -p 5000:5000 -v /path/to/osrm-data:/data osrm/osrm-backend osrm-routed --algorithm mld /data/serbia-latest.osrm
OSRM_URL=https://router.project-osrm.org

# Auth credentials
APP_USER=admin
APP_PASS=change-me
APP_USER2=dispatcher
APP_PASS2=change-me-too

# Python output buffering (keep as 1)
PYTHONUNBUFFERED=1

SESSION_COOKIE_SAMESITE=None
SESSION_COOKIE_SECURE=True
SECRET_KEY=any-long-random-string-here
